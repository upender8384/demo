---

-hosts : all

   tasks :


     -name: install nginx
      apt : pkg=nginx state=installed

    notify:
   -start nginx

handlers :

  -name: start nginx

 service: nginx status: starteD







   servicename=nainx state=starte
